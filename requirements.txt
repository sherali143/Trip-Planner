# Pinned to the versions the reported evaluation results were produced with.
# Commit 87b330c claimed a crewai pin that was never actually applied, so a
# fresh `pip install` picked up a different major version and broke the tool
# decorators. Do not loosen these without re-running comparison/run_comparison.py.
crewai==0.86.0
crewai-tools==0.76.0
litellm==1.74.9
mcp==1.28.1
python-dotenv==1.2.2
requests==2.32.5
pydantic==2.12.5
pyyaml==6.0.3
setuptools==65.5.0
langchain==1.3.14
streamlit==1.56.0
pytest==8.3.4

# Used by scripts/ to regenerate the figures and PROJECT_GUIDE.docx. These were
# missing: they happened to be present in one developer's global interpreter, so
# the generators worked there and failed in a clean virtual environment built
# from this file.
matplotlib==3.11.1
python-docx==1.2.0
